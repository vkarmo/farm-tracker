{
  "name": "farm-tracker-pwa",
  "version": "1.0.0",
  "scripts": {
    "build": "export PATH=\"$(brew --prefix node@22 2>/dev/null)/bin:$PATH\" && cd client && npm install --legacy-peer-deps && npm run build",
    "start": "NODE_ENV=production PORT=5000 node server/index.js",
    "dev": "export PATH=\"$(brew --prefix node@22 2>/dev/null)/bin:$PATH\" && concurrently \"npm start --prefix server\" \"npm run dev --prefix client\""
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.0",
    "nodemon": "^3.1.14"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
